import { createContext } from 'react';

export default createContext({
    errors: ['Heads up! A password reset link was sent to the email associated with your account: foo@bar.net'],
    messages: ['Heads up! A password reset link was sent to the email associated with your account: foo@bar.net'],
});
